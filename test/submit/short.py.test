from ghstack.test_prelude import *

await init_test()

await commit("A")
with captured_output() as (out, err):
    await gh_submit("Initial", short=True)
assert_eq(out.getvalue(), "https://github.com/pytorch/pytorch/pull/500\n")

ok()
