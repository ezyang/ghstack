from ghstack.test_prelude import *

await init_test()

# Narrow down the fetch on origin
await git(
    "config",
    "remote.origin.fetch",
    "+refs/heads/main:refs/remotes/origin/main",
)

await commit("A")
await gh_submit("Initial 1")

await amend("A2")
await gh_submit("Update 2")

ok()
